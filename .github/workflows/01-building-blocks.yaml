name: 01 - Building Blocks
on: push

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps:
            - name: Echo Hello
              run: echo "Hello, World!"
    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed Step
              run: | 
                echo "i exiting with error"
                exit 1
            - name: Echo Goodbye
              run: echo "Goodbye, World!"